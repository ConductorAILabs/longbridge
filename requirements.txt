# LongLive 2.0 MPS bridge — Mac/Apple Silicon dependencies
# Tested with Python 3.12.13 on macOS arm64.
#
# Skipped CUDA-only deps: flash-attn, nvidia-pyindex, nvidia-tensorrt,
# pycuda, torchao, decord (no arm64 wheel — stubbed in code).

torch==2.12.0
torchvision
torchaudio
diffusers==0.31.0
transformers>=4.49.0
tokenizers>=0.20.3
accelerate>=1.1.1

omegaconf
einops
av==13.1.0
imageio
imageio-ffmpeg
easydict
ftfy
sentencepiece
pydantic==2.10.6
huggingface_hub[cli]
peft
opencv-python
tqdm
lmdb
datasets
psutil
